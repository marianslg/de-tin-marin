---
const phone = "+54 123456789";                // número de contacto
const imageSrc = "/images/join-us.jpg";        // ruta de tu imagen
const imageAlt = "Unite a nuestra comunidad";  // texto alternativo
---

<section
  class="flex flex-col md:flex-row items-center px-32 gap-8 min-h-[70vh] py-16"
>
<!-- <div class="flex flex-col md:flex-row bg-white shadow-lg rounded-lg overflow-hidden"> -->
  <!-- Columna izquierda: Imagen -->
  <div class="md:w-1/2 h-64 md:h-auto">
    <img
      src={imageSrc}
      alt={imageAlt}
      class="w-full h-full object-cover"
    />
  </div>

  <!-- Columna derecha: Texto, formulario y contacto -->
  <div class="md:w-1/2 p-6 flex flex-col justify-center">
    <h2 class="text-3xl font-bold mb-4">Unite a nuestra comunidad</h2>
    <p class="mb-6">
      Suscribite a nuestro newsletter y enterate de todo lo nuevo que traemos,
      ofertas, descuentos ¡y más!
    </p>

    <form class="flex flex-col sm:flex-row gap-2 mb-8">
      <input
        type="email"
        placeholder="correo electrónico"
        required
        class="flex-1 px-4 py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-300"
      />
      <button
        type="submit"
        class="px-6 py-2 bg-green-300 text-black font-semibold rounded hover:bg-green-400 transition"
      >
        Suscribir
      </button>
    </form>

    <div>
      <h3 class="text-2xl font-bold mb-2">Contáctanos:</h3>
      <p class="text-xl">{phone}</p>
    </div>
  </div>
<!-- </div> -->
</section>